{
  "name": "safedrive-mern",
  "version": "1.0.0",
  "description": "Full MERN stack for SafeDrive Rewards",
  "private": true,
  "scripts": {
    
    "start": "set NODE_ENV=production&&node backend/server.js",
    "build": "npm run build-client && npm run build-server",
    "build-client": "cd client && npm install && npm run build",
    "build-server": "cd backend && npm install",

    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",
    "dev-server": "cd backend && npm run dev",
    "dev-client": "cd client && npm start",

    "server": "cd backend && npm run dev",
    "test": "cd backend && npm test",
    "lint": "cd backend && npm run lint",
    "format": "cd backend && npm run format",

    "install-all": "npm install && cd backend && npm install && cd ../client && npm install",
    "clean": "rm -rf node_modules && cd backend && rm -rf node_modules && cd ../client && rm -rf node_modules"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.x",
    "npm": ">=8.x"
  }
}